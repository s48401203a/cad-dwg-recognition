// 分享链接使用的主机名（可选）。
//
// 留空时服务端会自动探测本机局域网 IP（见 backend/api/projects.py 的 detect_lan_ip），
// 因此**通常不需要修改本文件**。只有在自动探测不可用、或需要固定某个内网主机名/域名时，
// 才填入 host（不要带协议与端口，也不要提交任何客户环境信息）。
//
// 注意：本文件随仓库公开分发，不要把真实内网地址、主机名或客户信息写进来。
window.CAD3D_SHARE = {
  host: "",
  hosts: [],
  updatedAt: ""
};
